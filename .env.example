# .env.example — скопируй в .env и заполни после запуска vault-init
# Эти значения генерируются автоматически скриптом scripts/start.sh

VAULT_ROLE_ID=<заполняется автоматически из certs/jenkins_role_id.txt>
VAULT_SECRET_ID=<заполняется автоматически из certs/jenkins_secret_id.txt>
